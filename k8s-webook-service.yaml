# yaml-language-server: $schema=.vscode/schemas/kubernetes-service.json
apiVersion: v1
kind: Service
metadata: 
  name: webook
spec:
  type: LoadBalancer
  selector:
    app: webook
  ports:    
    - protocol: TCP
      name: http
      port: 80
      targetPort: 8080  
  
